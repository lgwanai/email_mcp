# Email MCP Server Configuration Example
# Copy this file to .env and fill in your values

# Server Configuration
HOST=localhost
PORT=8000
TRANSPORT=sse
LOG_LEVEL=INFO

# Storage Configuration
ATTACHMENTS_DIR=attachments

# Email Server Configuration (Optional - can be auto-detected)
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USE_SSL=true

# Security Notes:
# - Never commit actual email credentials to version control
# - Email credentials are passed as parameters to the fetch_emails tool
# - Consider using app-specific passwords for Gmail and other providers